# Контур кафедры — передача

Актуальная документация: [HANDOFF.md](HANDOFF.md). В ней архитектура, функции, API, таблицы и связи, запуск, ограничения и инструкция для следующего AI. Дизайн: [DESIGN.md](DESIGN.md), [DESIGN_IMPLEMENTATION.md](DESIGN_IMPLEMENTATION.md), `.impeccable/design.json`. Правила работы: [AGENTS.md](AGENTS.md).

**Новая версия находится в локальной папке и архиве передачи, но ещё не отправлена в GitHub fork.** Точные пути указаны в HANDOFF.md.

Ниже сохранён исторический README исходного проекта. Его инструкция об удалении базы **отменена**: применённые миграции не переписывать, реальную базу не удалять; добавлять новые миграции и проверять на копии данных. Для запуска использовать HANDOFF.md.

---

# departament (историческая справка)

# 1. Проверить файл по пути `configs\default.yaml`
Там можно менять хост и порт

# 2. База данных находится по адресу `data\depatrament_data.db`
А ФАЙЛ МИГРАЦИИ ПО АДРЕСУ `internal\db\migration\20260325120000_init_schema.up.sql`

ЕСЛИ ЧТО ТО МЕНЯЕТСЯ В ФАЙЛЕ МИГРАЦИИ СЛЕДУЕТ УДАЛИТЬ СТАРУЮ БАЗУ ДАННЫХ/ЛИБО СМЕНИТЬ В ФАЙЛЕ КОНФИГУРАЦИИ ПУТЬ К БАЗЕ ДАННЫХ(ТОЛЬКО ИМЯ ИЗМЕНИТЬ)
```
db:
    local_path: "./data/depatrament_data.db"
```

# 3. ЗАПУСК ПРОЕКТА 
1. Для запуска необходимо установить компилятор `GO` версии `1.25.0` и выше.
2. Перейти в корень проекта
3. Из корня проекта выполнить команду
`go run main.go`

4. Дождаться запуска сервера
`{"L":"INFO","T":"2026-07-24T15:11:28.160+0300","M":"starting server","host":"localhost","port":"8080"}`

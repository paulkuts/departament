# Состояние проекта — 2026-09-11

## Доработки после развёртывания — 2026-09-12

- **Отмена утери ключа.** Пометка «утерян» стала обратимой: в карточке ключа кнопка превращается в «Отменить утерю», новое API `POST /keys/:id/restore` (admin) возвращает ключ в реестр как доступный, событие `restore` попадает в журнал. Повторная отмена — 409. В сервисный тест добавлена схема, повторяющая production (внешний ключ `key_logs.user_id → users`).
- **Исправлено молчаливое падение журнала утери.** События утери и отмены писались с пустым `user_id`, что нарушало внешний ключ: запись отклонялась, сервис ограничивался предупреждением в логе, и в журнале ключа этих операций не было вовсе. Теперь события записываются от имени администратора (в журнале виден автор операции). Исторические события утери за период до правки восстановлению не подлежат — их в базе нет.
- **HTTP-код повторной отмены** приведён к 409 вместо 500 (`not marked as lost` добавлен в маппинг ошибок).

## Продовой инстанс — 2026-09-12

Новая версия развёрнута на `rf-vps`: `https://mitm.kutspv.ru` (Caddy → 127.0.0.1:8085 → контейнер `departament-mitm`). Предыдущий инстанс на том же домене снят полностью, его данные и образ сохранены в `/root/backups/` на rf-vps. База новая и пустая. Рецепт развёртывания — `deploy/`; подробности в `deploy/README.md`. Источник развёрнутой сборки: ветка `codex/lab-notebook` форка `paulkuts/departament`, коммит снимка передачи плюс два исправления — `Secure: true` для refresh-cookie и рабочий `.gitignore` (исходный исключал `pkg/`, `go.mod`, `go.sum`, из-за чего репозиторий не собирался).

Передача 2026-09-12: редактирование сайта завершено по указанию владельца. Добавлены HANDOFF.md, AGENTS.md, DESIGN_IMPLEMENTATION.md и ссылка из README. Архив исходников: D:/Codex_Projects/Department/deliverables/department-handoff-2026-09-12.zip. Новые изменения в GitHub не отправлены. Ограничения production и права событий явно зафиксированы в HANDOFF.md; код при передаче не менялся.

## Рабочая копия

`D:/Codex_Projects/Department/.local/event-redesign`, ветка `codex/lab-notebook`, основа `event` / `bc77ea31630a9171b27df9b4bf43aa5b802de49a`.

Fork подтверждён: `paulkuts/departament`, parent `danrulev/departament`. `origin` указывает на fork, `upstream` — на исходный проект. Локальные изменения ещё не отправлены.

## Реализовано

- Новый frontend на native ES modules: постоянное меню, обзор, оборудование/инвентаризация, ключи, публикации, события, профили/роли, заметки, справочник, гостевые заявки, честная страница будущих ассистентов.
- Пользовательский текст строится DOM-методами, без HTML-интерполяции. Формы подписаны, диалоги native dialog, состояния ошибок/пустых данных, адаптивная компоновка.
- Регистрация staff, проверка пароля при входе, актуальные роль/активность из БД, проверки прав до изменения, права создателя статьи, собственные операции ключей.
- Новые таблицы приватных заметок, справочника, комментариев, выдачи оборудования, гостевых заявок и QR-ссылок.
- Миграции с ledger/checksum и транзакцией, исправленные SQLite pragmas, исключено создание встроенных аккаунтов при запуске.

## Локальная проверка

- `scripts/check.ps1`: Go tests, vet, build прошли.
- `scripts/smoke.py`: живые проверки API на отдельной demo.db, без вывода паролей и токенов.
- Все 38 проверок API прошли; финальная версия пересобрана и открыта на порту 18182. Проверены исправления навигации, обновлённые стили и отображение обзора.
- В браузере проверены обзор, карточка оборудования, сохранение и повторное открытие заметки; мобильная ширина 390px без горизонтального переполнения страницы.
- Impeccable detector по новой поверхности: замечаний нет.
- Ошибка первого входа относилась к невалидным ID синтетического seed. Генератор и тестовая база исправлены на UUID; связи проверены.

## Запуск

`scripts/setup-local.ps1` создаёт изолированную конфигурацию; `scripts/seed-demo.ps1` — синтетическую базу только для демонстрации. `scripts/dev.ps1` собирает приложение и запускает `http://127.0.0.1:18182/`. Go runtime и кэши находятся на D:. Созданные credentials находятся только в игнорируемом `.local/demo-credentials.json`.

Не запускать demo seed над реальными данными. Старое дерево `Department` с прежними незакоммиченными изменениями сохранено отдельно. GitHub push и deployment не выполнялись.

## Осталось за текущей рабочей версией

Production hardening и размещение, проверка миграции настоящей базы на её копии, реальное наполнение справочника. AI/сводки и совместный редактор — следующие этапы по PRODUCT.md. Полный аудит всех исторических backend-сценариев не заявляется выполненным.
